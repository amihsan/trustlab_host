from connectors.basic_connector import BasicConnector


class ChannelsConnector(BasicConnector):
    def register_at_director(self, director_hostname):
        # TODO open web socket
        # TODO handle incoming request

        # ip = '127.0.0.1'
        # buffer_size = 2048
        # tcp_server = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        # tcp_server.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
        # tcp_server.bind((ip, self.port))
        pass


